import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);

// Ensure the proxy is correctly set in package.json
// Check d:\VS Code Projects\My Financial Planner\frontend\package.json
